# Exported from Render on 2026-03-26T06:10:23Z
version: "1"
services:
  - type: web
    name: portal
    runtime: python
    repo: https://github.com/navdeepptu23-max/portal
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeployTrigger: commit
    envVars:
      - key: SECRET_KEY
        sync: false
